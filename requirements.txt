# Scraping and API Access
telethon
python-dotenv

# Database and ORM
psycopg2-binary
sqlalchemy

# Data Transformation (dbt)
dbt-postgres
# Note: dbt-utils is installed via dbt packages (see medical_warehouse/packages.yml)
# Run: dbt deps

# Data Enrichment (Object Detection)
ultralytics
opencv-python-headless
pandas

# API Development
fastapi
uvicorn[standard]
pydantic
slowapi

# Orchestration
dagster
dagster-webserver
dagster-postgres  # Recommended for database integration
dagster-dbt       # Recommended for dbt integration

# Testing
pytest
requests  # For API testing